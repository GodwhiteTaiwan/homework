{
  "name": "debt-ledger-workspace",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c cyan,magenta \"npm run dev --workspace @homework/api\" \"npm run dev --workspace @homework/web\"",
    "build": "npm run build --workspace @homework/api && npm run build --workspace @homework/web",
    "dev:api": "npm run dev --workspace @homework/api",
    "dev:web": "npm run dev --workspace @homework/web"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}