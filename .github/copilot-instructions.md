# Workspace Instructions

- Keep the frontend in `apps/web` and the backend in `apps/api`.
- Use SQLite for persistence and initialize the schema on API startup.
- Prefer small, focused changes that preserve the shadcn-style UI.
- Keep the app ready to run with `npm run dev` from the repository root.
